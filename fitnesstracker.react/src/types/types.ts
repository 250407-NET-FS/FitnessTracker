export interface Exercise {
    id: string;
    name: string;
    description: string;
    targetMuscleGroup: string;
    createdAt: string;
}

export interface User {
    id: string;
    name: string;
    email: string;
}